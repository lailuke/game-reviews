export const setPCState = updates => ({
  type: "ACTIVE_PC",
  updates
});

export const setConsoleState = updates => ({
  type: "ACTIVE_CONSOLE",
  updates
});

export const setVRState = updates => ({
  type: "ACTIVE_VR",
  updates
});
